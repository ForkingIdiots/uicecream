import { style } from "@vanilla-extract/css";

const button = style({
    backgroundColor: "red",
    color: "white",
    padding: "10px 20px",
    borderRadius: "5px",
});

export default { button };